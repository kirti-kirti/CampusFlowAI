package com.campusflow.ai.model;

/**
 * Roles available in CampusFlow AI.
 * Spring Security will prefix these with "ROLE_" automatically.
 */
public enum Role {
    ADMIN,
    TEACHER,
    STUDENT,
    PARENT
}
